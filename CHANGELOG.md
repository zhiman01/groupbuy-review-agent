# 团购复盘诊断 Agent 迭代记录

> 本文档记录团购复盘诊断 Agent 从 Workflow 底座到交互式 Agent 的完整迭代过程，包含扣子平台侧（Workflow / Botflow）与代码侧的全部变更。

---

## 平台侧迭代（扣子 Workflow & Botflow）

### WF-v1.0 — 批量复盘 Workflow 初版

**定位：** 固定的、批量的复盘交给 Workflow。入参一批商家 ID + 统计周期，拉指标 → 异常识别 → 根因诊断 → 生成报告，无对话能力。

- **新增** `groupbuy_review_workflow`（ID: `7670576870765592616`）
  - 输入：商家 ID 列表 + 统计周期
  - 流程：拉取团购指标（曝光、进店、核销、退款、差评、商圈对标）→ 指标异常识别 → 四类根因诊断（流量/转化/商品/服务品控）→ 输出结构化复盘建议报告
  - 内置合规校验节点：拦截越权建议（如直接命令"立即降价""马上下架"）
  - 约束：仅做归因分析，不直接下达调价或下架指令；所有建议需运营与商家人工确认

**测试结果：**
- ✅ 批量输入商家 ID，成功输出标准化复盘报告

---

### WF-v1.1 — 合规护栏 V1（固定词表拦截）

- **新增** 合规校验节点 V1：固定词表匹配
  - 拦截词：`立即降价`、`马上下架`、`必须调整` 等固定词组
  - 命中后替换为合规措辞或直接拦截

**测试结果（主动对抗测试）：**
- ❌ 发现漏拦：模型输出 `立即将套餐价调整`、`马上优化`，换了说法后固定词表匹配不上，护栏失效
- **归因：** 规则式合规的脆弱性——模型换一种表达就能绕过

---

### WF-v1.2 — 合规护栏 V2（强制信号词下沉替换）

- **优化** 合规校验节点 V1 → V2：词库从"固定词组"下沉到"强制信号词"
  - 拦截信号词与替换规则：

    | 拦截词（强制信号） | 替换为 |
    |-------------------|--------|
    | 立即 | 可评估 |
    | 马上 | 建议 |
    | 必须 | 建议 |
    | 务必 | 建议 |
    | 直接下架 | 评估套餐去留 |
    | 强制 | 建议 |

  - 替换原则：选择语法功能能对上的替换词（句法位置一致），保证替换后句子基本通顺
  - 示例：`立即降价` → `可评估调整价格`；`马上优化` → `建议优化`

**测试结果：**
- ✅ 对抗集中"立即""马上""必须"等强制语气均被成功替换
- ⚠️ 仍有边界 case：如 `建议强制优化出餐流程` 替换后变成 `建议建议优化出餐流程`（重复），复杂句式下粗暴替换有生硬问题
- **结论：** V2 解决了大部分明显越权，但天花板是规则式穷举不完，下一步需上 LLM 语义审查

---

### WF-v1.3 — 合规护栏 V3 设计（LLM 语义审查，已设计 Prompt，待部署）

- **设计** 合规校验节点 V3：LLM 语义审查，理解语义而非匹配关键词
  - V3 Prompt：

    ```
    你是本地生活团购复盘的合规审查员。
    下面是 AI 生成的一段复盘建议。请判断它是否越权——即是否在替商家下达强制决策，而非提供参考建议。

    越权的判定标准（满足任一即为越权）：
    1. 使用强制/命令语气要求商家执行某动作（如"立即降价""必须下架"）；
    2. 直接替商家做出调价、下架、改套餐等经营决策，而非建议其评估；
    3. 未标注"需运营与商家确认"就给出调价/下架类结论。

    请输出 JSON：
    {
      "is_violation": true/false,
      "reason": "判定依据，一句话",
      "rewrite": "若越权，把越权表述改写成合规的建议措辞；若不越权，原样返回"
    }
    ```

  - 架构定位：V2 做前置快拦（成本低、速度快），V3 做后置审查（兜住 V2 漏掉的语义级越权），双层互补

**状态：** Prompt 已设计完成，待部署到 Workflow 节点并回归测试

---

### BF-v1.0 — 交互式诊断 Agent（Botflow）初版

**定位：** 灵活的、交互的深挖交给 Agent。运营针对某一家店深度追问，Agent 判断信息是否充分，不够就追问，够了按需调用底层 Workflow。

- **新增** `groupbuy_review_agent`（ID: `7670716918810443818`）
  - 输入：`USER_INPUT`（运营提问）+ `CONVERSATION_NAME`（会话上下文绑定）
  - 输出：`output`（文本回复）
  - 核心能力：
    - 信息充分性判断：缺失关键信息（如近期是否改价/换套餐图）时主动追问
    - 按需调用底层 `groupbuy_review_workflow` 生成完整复盘
    - 单指标深度归因：不需要完整复盘时直接分析单个指标
  - 双输出结构：
    - 【运营内部分析】：专业直接，含数据引用和根因判断
    - 【可转发给商家的沟通话术】：友好、只讲方向，不含内部指标细节
  - 合规约束：
    - 禁止直接命令语气（"立即降价""马上下架"），使用"可考虑""建议评估"
    - 调价/下架类建议必须标注"需运营与商家确认"
    - 优惠、指标只能引用真实数据，信息不足声明缺口，不编造

**Prompt 核心规则（4 条行为约束）：**
1. **信息不足先追问** — 不够就先问"近期是否改过价、换过套餐图"，不硬下结论
2. **双输出** — 强制分"运营内部分析"和"商家话术"两块，避免运营自行改写产生误解
3. **合规措辞** — 禁止强制指令，调价下架类建议必须标注需确认
4. **反幻觉** — 优惠/指标只引用真实数据，信息不足声明缺口

**测试结果：**
- ✅ 输入"烧烤店A核销为什么下滑"，Agent 主动声明信息缺口（需验证套餐改动记录、退款订单原因、商圈竞品曝光量），未硬编结论
- ✅ 商家话术中使用"您可以检查最近是否调整过价格"友好嵌入追问

---

## 代码侧迭代（本地 Agent 集成）

### v1.0.0 — 2026-08-06

**初始化：本地 Agent 集成扣子平台工作流**

- **新增** `src/tools/groupbuy_review_tool.py`：封装扣子平台 `groupbuy_review_agent` 工作流（ID: `7670716918810443818`）调用逻辑
  - 支持 `USER_INPUT`（用户提问）和 `CONVERSATION_NAME`（会话上下文绑定）参数
  - 通过 HTTP OpenAPI 同步调用工作流
  - 包含完整的错误处理：网络异常、HTTP 状态码校验、JSON 解析异常、业务错误码
  - 请求耗时日志、响应状态码日志
- **新增** `src/agents/agent.py`：Agent 核心逻辑
  - 基于 `doubao-seed-2-0-lite-260215` 模型
  - 集成 `groupbuy_review` 工具
  - 工具调用错误中间件（`handle_tool_errors`），防止工具异常阻塞 Agent 循环
  - 短期记忆：滑动窗口保留最近 20 轮对话（40 条消息）
- **新增** `config/agent_llm_config.json`：模型配置与系统提示词
  - 角色：平台生活服务运营智能助手
  - 职责：理解运营提问 → 调用诊断工具 → 呈现结果
  - 输出结构：【运营内部分析】+【可转发给商家的沟通话术】

**测试结果：**
- ✅ 输入"商家ID 12345 核销率"，成功调用工作流并返回完整复盘诊断

---

### v1.0.1 — 2026-08-06

**优化：工具健壮性增强**

- **修复** `src/tools/groupbuy_review_tool.py`：
  - 新增 HTTP 状态码校验（非 200 时直接返回错误，避免无效 JSON 解析）
  - 新增 `requests.exceptions.RequestException` 捕获（网络超时、连接失败等）
  - 新增 JSON 解析异常捕获（`ValueError`）
  - 日志增强：记录请求耗时、响应状态码、输入长度、错误响应体（截取前 500 字符）

**测试结果：**
- ✅ 正常调用成功返回诊断结果
- ✅ 平台临时报错时正确捕获并返回友好提示（未崩溃）

---

<!-- 
模板：每次迭代新增一个条目，格式如下

## 平台侧：WF-vX.Y / BF-vX.Y — YYYY-MM-DD
## 代码侧：vX.Y.Z — YYYY-MM-DD

### 新增 / 优化 / 修复

- **新增/修复/优化** `文件路径` 或 `平台资产名`：变更描述

### 测试结果

- ✅ / ❌ / ⚠️ 测试情况
-->
